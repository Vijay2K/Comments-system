<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Showing an unique comment</title>
</head>
<body>
    <h4><%= `userID : ${comment.id}` %> </h4>
    <h4><%= `User Name : ${comment.username}` %> </h4>
    <h4><%= `comment : ${comment.comment}` %> </h4>
    <form method="POST" action="/comments/<%= comment.id %>?_method=DELETE ">
        <button>Delete</button>
    </form>
    <a href="/comments">Back to comments</a><br>
    <a href="/comments/new">Back to Make Comments</a>
</body>
</html>